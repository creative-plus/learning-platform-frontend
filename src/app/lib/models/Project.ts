export interface Project {
  id?: number;
  name: string;
  description: string;
  startDate: string;
  endDate: string;
  totalBudget: number;
  financeMean: string;
  minAge: number;
  maxAge: number;
}